var chai = require('chai');
var expect = chai.expect;

describe('mocha-test #1', function () {

    it ('simple test #1.1', function (done) {
        expect(true).to.be.true;
        done();
    });

    it ('simple test #1.1', function (done) {
        expect(true).to.be.true;
        done();
    });
    
});